[project]
name = "bob-agentic-ai-workshop"
version = "0.1.0"
description = "IBM PowerPoint presentation generator"
requires-python = ">=3.13"
dependencies = [
    "lxml>=5.3.0",
    "python-pptx==1.0.2",
]

# Match the project runtime requirement.
target-version = "py313"
